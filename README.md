# Cravio JO

## Project structure

- `main.html`: Main landing page file (moved from `src/index.html`).
- `data/`: Reference and research documents.

## Notes

- The `src/` directory has been removed; the landing page now lives at the project root.